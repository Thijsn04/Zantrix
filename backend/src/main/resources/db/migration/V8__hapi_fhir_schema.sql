-- HAPI FHIR Schema is managed by Hibernate auto-ddl or HAPI SchemaMigrator
-- due to its massive size (hundreds of tables). 
-- This script acts as a placeholder to maintain Flyway versioning.
SELECT 1;
